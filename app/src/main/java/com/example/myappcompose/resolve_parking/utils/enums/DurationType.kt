package com.example.myappcompose.resolve_parking.utils.enums

enum class DurationType(val id: Int, val text: String) {
    Normal(1, "Normal Duration"),
    AllDay(2, "All Day"),
    Overnight(3, "Overnight")
}